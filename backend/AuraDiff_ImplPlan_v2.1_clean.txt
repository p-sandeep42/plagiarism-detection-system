AURADIFFRevised Implementation PlanBatch-First Edition  ·  v2.1Incorporating all SOP v2.0 corrections, gap-fills, and priority sequencingPriority Order of Implementation① PHASE 1 — Batch Core (§5)② PHASE 2 — Frontend & SEO③ PHASE 3 — Infra & ScalingTeamP. SandeepMahathiLokeshPramodInstitutionVignan Institute of Technology & ScienceDept. of Computer Science & EngineeringB.Tech CSE  ·  2nd Year§0  What Changed From the Original PlanThis section documents every correction, addition, and structural change made relative to the original plan. Use it as a quick diff before briefing any AI agent.AreaOriginal Plan SaidRevised Plan Corrects Tonext.config.tsNot mentioned[NEW] required — env vars + CORS proxy to FastAPIrequirements.txtNot mentioned[MODIFY] pin websockets==12.0, httpx==0.27.0/compare-batchUse asyncio.gather over /compareCall algorithm functions directly, NEVER loop the /compare handlerWebSocket authNo auth specifiedToken param: /ws/batch-progress?token=SESSION_TOKENStudentDetailPanel tab 2'Imports SplitView.tsx'Explicitly maps PairScore.highlights → SplitView highlightData propBatchUploadZoneEmbedded in batch/page.tsxExtracted to components/batch/BatchUploadZone.tsx (separate component)MAX_BATCH_FILESHardcoded 30Env var MAX_BATCH_FILES=30 in .env.local and FastAPI settingsWeight sum testNot in verification planUnit test tests/test_weight_sum.py added to verification checklistError boundaryNot mentionedBatchErrorBoundary.tsx wrapping StudentMatrix to prevent full-page crashFirebase stubMinimal mentionfirebase.ts has full typed method stubs so TS compiler catches mismatchesPHASE 1 — Batch Core Feature  [HIGHEST PRIORITY]All items in Phase 1 must be completed and verified before Phase 2 begins. These items are interdependent — back-end first, then API layer, then UI.P1-A  Backend SchemasP1-B  FastAPI EndpointsP1-C  Next.js ConfigP1-D  API RoutesP1-E  UI ComponentsP1-F  Batch PageP1-A  [MODIFY] backend/models/schemas.pyAdd the four new Pydantic models below. Place them AFTER the existing ComparisonResponse class. Do NOT modify any existing class.Exact schema additions — copy verbatimclass StudentMeta(BaseModel):    student_index: int    name: str          # derived from filename, e.g. 'sandeep' from 'sandeep.py'    filename: strclass PairScore(BaseModel):    winnowing_score: float    jaccard_score: float    semantic_score: float    total_score: float    highlights: List[HighlightInfo]  # reuse existing HighlightInfoclass StudentSummary(BaseModel):    student_index: int    max_score: float    # highest pairwise total_score against any peer    avg_score: float    # mean of all pairwise total_scores    risk_level: str     # 'Low'|'Medium'|'High'|'Critical'class BatchComparisonResponse(BaseModel):    students: List[StudentMeta]    matrix: List[List[Optional[PairScore]]]   # n×n; None on diagonal    summary: List[StudentSummary]              # sorted by max_score descRisk level thresholds for StudentSummary (implement in a helper function, not inline):max_score < 0.30  →  'Low'  (green)max_score 0.30–0.60  →  'Medium'  (yellow)max_score 0.61–0.79  →  'High'  (orange)max_score >= 0.80  →  'Critical'  (red)P1-B  [MODIFY] backend/main.pyAdd three items to main.py: a startup model-load event, the /compare-batch POST endpoint, and the /ws/batch-progress WebSocket endpoint.⚠️  CRITICAL — Do NOT loop /compare. Call algorithm functions directly.# WRONG (never do this):for i, j in pairs:    result = await compare(file_i, file_j)   # <-- loops the HTTP handler, WRONG# CORRECT — import and call the algorithm functions directly:from algorithms.winnowing  import compute as winnowing_comparefrom algorithms.structural import compute as structural_comparefrom algorithms.semantic   import compute as semantic_compareasync def _run_pair(text_a, text_b, type_a, type_b):    loop = asyncio.get_event_loop()    w = await loop.run_in_executor(pool, winnowing_compare,  text_a, text_b)    s = await loop.run_in_executor(pool, structural_compare, text_a, text_b, type_a, type_b)    e = await loop.run_in_executor(pool, semantic_compare,   text_a, text_b)    total = _aggregate(w, s, e, type_a)   # existing weight logic    return PairScore(winnowing_score=w, jaccard_score=s, semantic_score=e,                     total_score=total, highlights=_get_highlights(text_a,text_b))/compare-batch endpoint specification@app.post('/compare-batch', response_model=BatchComparisonResponse)async def compare_batch(files: List[UploadFile] = File(...)):    # Guard: 2 <= n <= MAX_BATCH_FILES    n = len(files)    if n < 2 or n > int(os.getenv('MAX_BATCH_FILES', 30)):        raise HTTPException(400, detail='Requires 2–30 files')    # 1. Parse all files (reuse existing parse_file() helper)    parsed = [parse_file(f) for f in files]   # (text, file_type) tuples    students = [StudentMeta(student_index=i,                 name=_name_from_filename(files[i].filename),                 filename=files[i].filename) for i in range(n)]    # 2. Generate all unique pairs and run concurrently    pairs = list(itertools.combinations(range(n), 2))    tasks = [_run_pair(*parsed[i], *parsed[j]) for i,j in pairs]    results = await asyncio.gather(*tasks)    # 3. Fill n×n matrix (None on diagonal)    matrix = [[None]*n for _ in range(n)]    for (i,j), res in zip(pairs, results):        matrix[i][j] = res        matrix[j][i] = res   # symmetric    # 4. Build summary sorted by max_score desc    summary = sorted([_build_summary(i, matrix, n) for i in range(n)],                     key=lambda s: s.max_score, reverse=True)    return BatchComparisonResponse(students=students, matrix=matrix, summary=summary)/ws/batch-progress WebSocket specification# Token validation guard (prevents unauthenticated connections)@app.websocket('/ws/batch-progress')async def batch_progress_ws(ws: WebSocket, token: str = Query(...)):    if not _validate_session_token(token):        await ws.close(code=1008)   # Policy Violation        return    await ws.accept()    # Client sends: { 'session_id': str }    # Server emits: { 'completed': int, 'total': int, 'current_pair': [i,j] }    # Use asyncio.Queue stored in a module-level dict keyed by session_id    # _run_pair() puts progress updates into the queue while processing    ...# Add to requirements.txt:# websockets==12.0# python-jose==3.3.0   (for token validation)Additional required additions to main.py:Import: import itertools, asyncio, os, concurrent.futuresStartup event: load sentence-transformers model once into module-level variable MODELThread pool: pool = concurrent.futures.ThreadPoolExecutor(max_workers=4) at module levelHelper _name_from_filename(filename): strip extension, replace underscores/hyphens with spaces, title-caseHelper _validate_session_token(token): check against a simple env-var secret for now (BATCH_WS_SECRET)Helper _build_summary(i, matrix, n): compute max_score, avg_score, risk_level for student iP1-C  [NEW] frontend/next.config.ts  ← was missing from original planThis file is required for environment variable exposure, the FastAPI proxy (prevents CORS in production), and the image domain allowlist.next.config.ts — complete fileimport type { NextConfig } from 'next';const nextConfig: NextConfig = {  env: {    NEXT_PUBLIC_BACKEND: process.env.NEXT_PUBLIC_BACKEND ?? 'fastapi',    NEXT_PUBLIC_FASTAPI_URL: process.env.NEXT_PUBLIC_FASTAPI_URL ?? 'http://localhost:8000',    MAX_BATCH_FILES: process.env.MAX_BATCH_FILES ?? '30',  },  async rewrites() {    return [      { source: '/fastapi/:path*',        destination: `${process.env.NEXT_PUBLIC_FASTAPI_URL}/:path*` },    ];  },  images: { domains: ['localhost'] },};export default nextConfig;// Also create .env.local (gitignored) with:// NEXT_PUBLIC_BACKEND=fastapi// NEXT_PUBLIC_FASTAPI_URL=http://localhost:8000// MAX_BATCH_FILES=30// BATCH_WS_SECRET=replace_with_random_32_char_stringP1-D  API Routes & Abstraction Layer[NEW] frontend/src/app/api/compare-batch/route.tsActs as a secure server-side proxy. The client never calls FastAPI directly, so the FastAPI URL is never exposed to the browser.compare-batch/route.tsimport { NextRequest, NextResponse } from 'next/server';export async function POST(req: NextRequest) {  const formData = await req.formData();  const fastapiUrl = process.env.NEXT_PUBLIC_FASTAPI_URL;  try {    const upstream = await fetch(`${fastapiUrl}/compare-batch`, {      method: 'POST', body: formData,    });    if (!upstream.ok) {      const err = await upstream.text();      return NextResponse.json({ error: err }, { status: upstream.status });    }    const data = await upstream.json();    return NextResponse.json(data);  } catch (e) {    return NextResponse.json({ error: 'Backend unreachable' }, { status: 502 });  }}[NEW] frontend/src/lib/backend.ts  +  backends/fastapi.ts  +  backends/firebase.tslib/backend.ts — factory and interfaceimport type { ComparisonResponse } from '@/types/comparison';import type { BatchComparisonResponse } from '@/types/batch';export interface BackendService {  compare(fileA: File, fileB: File): Promise<ComparisonResponse>;  compareBatch(files: File[]): Promise<BatchComparisonResponse>;}// Lazy imports to avoid bundling both backendsexport async function getBackend(): Promise<BackendService> {  if (process.env.NEXT_PUBLIC_BACKEND === 'firebase') {    const { FirebaseBackend } = await import('./backends/firebase');    return new FirebaseBackend();  }  const { FastAPIBackend } = await import('./backends/fastapi');  return new FastAPIBackend();}backends/fastapi.tsexport class FastAPIBackend implements BackendService {  async compare(fileA: File, fileB: File): Promise<ComparisonResponse> {    const fd = new FormData();    fd.append('file_a', fileA); fd.append('file_b', fileB);    const res = await fetch('/api/compare', { method: 'POST', body: fd });    if (!res.ok) throw new Error(await res.text());    return res.json();  }  async compareBatch(files: File[]): Promise<BatchComparisonResponse> {    const fd = new FormData();    files.forEach(f => fd.append('files', f));    const res = await fetch('/api/compare-batch', { method: 'POST', body: fd });    if (!res.ok) throw new Error(await res.text());    return res.json();  }}backends/firebase.ts — fully typed stub (prevents TypeScript drift)// Stub: swap with real Firebase callable when ready.// All method signatures MUST stay in sync with BackendService interface.export class FirebaseBackend implements BackendService {  async compare(_fileA: File, _fileB: File): Promise<ComparisonResponse> {    // TODO: import { getFunctions, httpsCallable } from 'firebase/functions'    // const fn = httpsCallable(functions, 'compareDocuments');    // return fn({ ... }) as Promise<ComparisonResponse>;    throw new Error('FirebaseBackend.compare not yet implemented');  }  async compareBatch(_files: File[]): Promise<BatchComparisonResponse> {    throw new Error('FirebaseBackend.compareBatch not yet implemented');  }}P1-E  [NEW] Batch UI Components[NEW] components/batch/BatchUploadZone.tsx  ← extracted from page (was missing)Separating the upload zone into its own component keeps batch/page.tsx readable and makes the zone independently testable.Props: onFilesAccepted(files: File[]): void, maxFiles: numberFeatures: drag-and-drop via HTML5 API, click-to-browse fallback, file type guard (.py .js .txt .pdf .docx), file count badge, remove-individual-file buttonShow a filename chip for each accepted file. Derive student name from filename (strip extension, split on _ or -)Framer Motion: each chip animates in with layout animation (AnimatePresence + motion.div)If files.length > maxFiles: show inline error badge, disable submit CTADo NOT put axios/fetch calls in this component. It is display-only.[NEW] components/batch/BatchErrorBoundary.tsx  ← was missing from original planWraps StudentMatrix so a single-file parse failure cannot crash the entire /batch page.BatchErrorBoundary.tsxexport class BatchErrorBoundary extends React.Component<  { children: React.ReactNode },  { hasError: boolean; error: Error | null }> {  state = { hasError: false, error: null };  static getDerivedStateFromError(error: Error) {    return { hasError: true, error };  }  render() {    if (this.state.hasError) return (      <div className='p-6 rounded-xl border border-red-300 bg-red-50 text-red-700'>        <p className='font-semibold'>Comparison failed</p>        <p className='text-sm'>{this.state.error?.message}</p>      </div>    );    return this.props.children;  }}[NEW] components/batch/StudentMatrix.tsxRenders the N×N pairwise heatmap grid. Full specification from SOP §5.2, with corrected prop interface:Props: data: BatchComparisonResponse, onStudentClick(studentIndex: number): voidGrid: CSS Grid with auto columns. Column/row headers = student names.Cell colour: score < 0.30 → bg-green-100, 0.30–0.60 → bg-yellow-100, 0.61–0.79 → bg-orange-100, >= 0.80 → bg-red-200Diagonal: render '--' in slate-300, no click handlerHover tooltip (Tailwind group-hover): shows winnowing / structural / semantic breakdownClick: calls onStudentClick(row_index) — the panel shows the clicked student's dataSort toggle: button to sort student rows by max_score desc / alphabeticalExport: 'Download CSV' button — generates pairwise matrix as CSV client-side (no API call)[NEW] components/batch/StudentDetailPanel.tsxSide-drawer with 3 tabs. Critical correction: Tab 2 must explicitly map PairScore fields to SplitView props — the original plan omitted this mapping.StudentDetailPanel — corrected prop mapping for Tab 2 (SplitView)// The existing SplitView expects these props:// highlightData: HighlightInfo[]// sourceText: string// targetText: string// From BatchComparisonResponse, get the worst-case pair for this student:const worstPair = getWorstPair(studentIndex, batchResponse.matrix);// worstPair is a PairScore — it has a .highlights field// Correct mapping:<SplitView  highlightData={worstPair.highlights}       // HighlightInfo[] — same type  sourceText={parsedTexts[studentIndex]}     // string — student's file text  targetText={parsedTexts[worstPeer.index]}  // string — worst-match peer text/>// NOTE: parsedTexts must be returned from the backend in BatchComparisonResponse// Add: parsed_texts: List[str]  to BatchComparisonResponse schema (P1-A)Remaining StudentDetailPanel specifications:Props: studentIndex: number, data: BatchComparisonResponse, parsedTexts: string[], onClose(): voidTab 1 — Score Overview: renders <Dashboard> with worst-case PairScore injected as scores propTab 2 — Side-by-Side: renders <SplitView> using mapping aboveTab 3 — All Comparisons: table of all peers sorted by pairwise score desc + mini progress-bar per rowAnimation: Framer Motion — initial={{ x: '100%' }} animate={{ x: 0 }} exit={{ x: '100%' }}Backdrop: semi-transparent slate-900/60 overlay behind the panel, click-away closesHeader: student name, filename chip, risk-level badge (colour matches matrix cell colour)P1-F  [NEW] frontend/src/app/batch/page.tsxThe batch route page wires everything together. Keep this file thin — all complexity lives in the sub-components.Imports: BatchUploadZone, BatchErrorBoundary, StudentMatrix, StudentDetailPanelState: files[], batchResult: BatchComparisonResponse|null, selectedStudent: number|null, isLoading: boolean, progress: {completed,total}On mount: open WebSocket to /ws/batch-progress?token=SESSION_TOKEN; update progress state on each messageOn submit: call getBackend().compareBatch(files), set batchResultRender sequence: (1) BatchUploadZone, (2) progress bar (visible only when isLoading), (3) BatchErrorBoundary > StudentMatrix, (4) StudentDetailPanel (conditional)SEO: export const metadata = { title: 'Batch Compare | AuraDiff', description: '...' }PHASE 2 — Frontend, SEO & BrandingBegin Phase 2 only after Phase 1 is verified. Items in Phase 2 that depend on Phase 1 are marked with a dependency flag.P2-A  Configuration[NEW] frontend/src/styles/tokens.css — CSS variables per SOP §1.1 design token table[MODIFY] frontend/src/app/layout.tsx — Next.js Metadata API (see SOP §3.1), import tokens.css globally[NEW] frontend/public/logo.svg — SVG wordmark (AURA in Indigo-600, DIFF in Cyan-500)[NEW] frontend/src/app/sitemap.ts — dynamic sitemap including /, /compare, /batch, /about[NEW] frontend/src/app/robots.ts — allow all, disallow /api/*P2-B  Landing Page Overhaul  (frontend/src/app/page.tsx)Hero section — Framer Motion stagger headline, particle background, spring-physics dashboard previewAlgorithm Showcase — 3 live-demo cards (Winnowing / Structural / Semantic per SOP §2.2)Statistics bar — CountUp hook, animated on scroll-enterCreator section — 4 team cards with stagger delay (Sandeep 0s, Mahathi 0.1s, Lokesh 0.2s, Pramod 0.3s)All sections: Framer Motion sectionVariant (initial opacity:0 y:32 → visible opacity:1 y:0)Dependency flag: batch CTA button links to /batch — requires Phase 1 completeP2-C  SEOJSON-LD: SoftwareApplication schema on layout, FAQPage on /about, BreadcrumbList on /compare and /batchnext/font: load Inter with display:swap, pass to --font-sans tokenAll Next.js <Image> components: add priority prop to above-fold imagesCore Web Vitals: set explicit min-height on all animated section containers to prevent CLSPHASE 3 — Infrastructure & ScalingP3-A  [NEW] backend/DockerfileProduction DockerfileFROM python:3.11-slimWORKDIR /appCOPY requirements.txt .RUN pip install --no-cache-dir -r requirements.txtCOPY . .# Pre-download the sentence-transformers model at build timeRUN python -c "from sentence_transformers import SentenceTransformer; SentenceTransformer('all-MiniLM-L6-v2')"EXPOSE 8000CMD ["gunicorn", "-k", "uvicorn.workers.UvicornWorker", "-w", "4", "-b", "0.0.0.0:8000", "main:app"]P3-B  [MODIFY] backend/requirements.txt  ← was missing from original planAdd and pin: websockets==12.0Add and pin: python-jose==3.3.0Add and pin: httpx==0.27.0  (used for any outbound requests)Verify existing: sentence-transformers==2.7.0, fastapi, uvicorn, gunicorn — pin allP3-C  MonitoringAdd Sentry SDK to both Next.js (sentry.client.config.ts) and FastAPI (main.py)Add Vercel Analytics: import { Analytics } from '@vercel/analytics/react' in layout.tsxVerification Plan  (Revised)Run verifications in order. Do not advance to the next phase until all checks in the current phase pass.Phase 1 VerificationsRun tests/test_weight_sum.py — assert winnowing + structural + semantic weights sum to 1.0  ← NEW, was missingStart FastAPI: uvicorn main:app --reload. Send 3 files to POST /compare-batch. Assert response shape matches BatchComparisonResponse schema.Run python tests/test_batch_ws.py — connects WebSocket, sends session_id, asserts progress messages arrive as { completed, total, current_pair }.Assert /compare (existing) still returns ComparisonResponse unchanged — regression check.Run next build — assert no TypeScript errors in batch/ or lib/backends/Phase 2 VerificationsOpen /batch in browser. Upload 3 dummy files (e.g. hello.py, world.py, sample.txt). Confirm matrix renders with correct colour cells.Click a student name. Confirm StudentDetailPanel slides in from the right. Confirm all 3 tabs render without errors.Verify Tab 2 (SplitView) shows highlights coloured correctly (Blue=Exact, Emerald=Structural, Purple=Semantic).Scroll the landing page. Confirm each section fades-in from y:32 on scroll-enter (not on page load).Run Lighthouse on /. Target: Performance >= 85, SEO >= 95.Phase 3 Verificationsdocker build -t auradiff-backend . — assert build completes, model downloads at build time.docker run -p 8000:8000 auradiff-backend — assert /compare-batch accessible.npx @next/bundle-analyzer — assert first-load JS < 150kB.Master Implementation ChecklistEvery checkbox below must be ticked before the project is considered complete. Items in red were absent from the original plan.☐PhaseTaskRef☐P1schemas.py — 4 new models (StudentMeta, PairScore, StudentSummary, BatchComparisonResponse)P1-A☐P1schemas.py — parsed_texts: List[str] added to BatchComparisonResponse  ← NEWP1-A☐P1main.py — startup event loads SentenceTransformer model onceP1-B☐P1main.py — ThreadPoolExecutor pool at module levelP1-B☐P1main.py — _run_pair() calls algorithm functions directly, NOT /compare handler  ← NEWP1-B☐P1main.py — POST /compare-batch endpointP1-B☐P1main.py — WebSocket /ws/batch-progress with token validation  ← NEWP1-B☐P1requirements.txt — websockets==12.0, python-jose==3.3.0 pinned  ← NEWP1-B☐P1next.config.ts — env vars, FastAPI rewrite proxy, image domains  ← NEWP1-C☐P1.env.local created with all required variables  ← NEWP1-C☐P1app/api/compare-batch/route.ts — Next.js server-side proxyP1-D☐P1lib/backend.ts — BackendService interface + getBackend() factoryP1-D☐P1lib/backends/fastapi.ts — fully typed implementationP1-D☐P1lib/backends/firebase.ts — fully typed stub (throws, not silent)  ← NEWP1-D☐P1BatchUploadZone.tsx — extracted as separate component  ← NEWP1-E☐P1BatchErrorBoundary.tsx — wraps StudentMatrix  ← NEWP1-E☐P1StudentMatrix.tsx — N×N heatmap, click handler, CSV exportP1-E☐P1StudentDetailPanel.tsx — 3-tab drawer with corrected SplitView prop mapping  ← NEWP1-E☐P1app/batch/page.tsx — wires all components, WebSocket progressP1-F☐P1tests/test_weight_sum.py passes  ← NEWVerify☐P2styles/tokens.css — all design tokensP2-A☐P2logo.svg in /publicP2-A☐P2layout.tsx — Next.js Metadata API, JSON-LDP2-A☐P2sitemap.ts + robots.tsP2-A☐P2page.tsx — hero, algo showcase, stats, creator section, scroll animationsP2-B☐P3backend/DockerfileP3-A☐P3Bundle < 150kB first-load JSP3-CAuraDiff Revised Implementation Plan v2.1 — Batch-First Edition. Vignan Institute of Technology and Science, CSE Department. P. Sandeep · Mahathi · Lokesh · Pramod.