AURA DIFF Revised Implementation Plan Batch-First Edition · v2.1 Incorporating all SOP v2.0 corrections, gap-fills, and priority sequencing Priority Order of Implementation ① PHASE 1 — Batch Core (§5) ② PHASE 2 — Frontend &amp; SEO ③ PHASE 3 — Infra &amp; Scaling Team P. Sandeep Mahathi Lokesh Pramod Institution Vignan Institute of Technology &amp; Science Dept. of Computer Science &amp; Engineering B.Tech CSE · 2nd Year §0 What Changed From the Original Plan This section documents every correction, addition, and structural change made relative to the original plan. Use it as a quick diff before briefing any AI agent. Area Original Plan Said Revised Plan Corrects To next.config.ts Not mentioned [NEW] required — env vars + CORS proxy to FastAPI requirements.txt Not mentioned [MODIFY] pin websockets==12.0, httpx==0.27.0 /compare-batch Use asyncio.gather over /compare Call algorithm functions directly, NEVER loop the /compare handler WebSocket auth No auth specified Token param: /ws/batch-progress?token=SESSION_TOKEN StudentDetailPanel tab 2 &apos;Imports SplitView.tsx&apos; Explicitly maps PairScore.highlights → SplitView highlightData prop BatchUploadZone Embedded in batch/page.tsx Extracted to components/batch/BatchUploadZone.tsx (separate component) MAX_BATCH_FILES Hardcoded 30 Env var MAX_BATCH_FILES=30 in .env.local and FastAPI settings Weight sum test Not in verification plan Unit test tests/test_weight_sum.py added to verification checklist Error boundary Not mentioned BatchErrorBoundary.tsx wrapping StudentMatrix to prevent full-page crash Firebase stub Minimal mention firebase.ts has full typed method stubs so TS compiler catches mismatches PHASE 1 — Batch Core Feature [HIGHEST PRIORITY] All items in Phase 1 must be completed and verified before Phase 2 begins. These items are interdependent — back-end first, then API layer, then UI. P1-A Backend Schemas P1-B FastAPI Endpoints P1-C Next.js Config P1-D API Routes P1-E UI Components P1-F Batch Page P1-A [MODIFY] backend/models/schemas.py Add the four new Pydantic models below. Place them AFTER the existing ComparisonResponse class. Do NOT modify any existing class. Exact schema additions — copy verbatim class StudentMeta(BaseModel): student_index: int name: str # derived from filename, e.g. &apos;sandeep&apos; from &apos;sandeep.py&apos; filename: str class PairScore(BaseModel): winnowing_score: float jaccard_score: float semantic_score: float total_score: float highlights: List[HighlightInfo] # reuse existing HighlightInfo class StudentSummary(BaseModel): student_index: int max_score: float # highest pairwise total_score against any peer avg_score: float # mean of all pairwise total_scores risk_level: str # &apos;Low&apos;|&apos;Medium&apos;|&apos;High&apos;|&apos;Critical&apos; class BatchComparisonResponse(BaseModel): students: List[StudentMeta] matrix: List[List[Optional[PairScore]]] # n×n; None on diagonal summary: List[StudentSummary] # sorted by max_score desc Risk level thresholds for StudentSummary (implement in a helper function, not inline): max_score &lt; 0.30 → &apos;Low&apos; (green) max_score 0.30–0.60 → &apos;Medium&apos; (yellow) max_score 0.61–0.79 → &apos;High&apos; (orange) max_score &gt;= 0.80 → &apos;Critical&apos; (red) P1-B [MODIFY] backend/main.py Add three items to main.py: a startup model-load event, the /compare-batch POST endpoint, and the /ws/batch-progress WebSocket endpoint. ⚠️ CRITICAL — Do NOT loop /compare. Call algorithm functions directly. # WRONG (never do this): for i, j in pairs: result = await compare(file_i, file_j) # &lt;-- loops the HTTP handler, WRONG # CORRECT — import and call the algorithm functions directly: from algorithms.winnowing import compute as winnowing_compare from algorithms.structural import compute as structural_compare from algorithms.semantic import compute as semantic_compare async def _run_pair(text_a, text_b, type_a, type_b): loop = asyncio.get_event_loop() w = await loop.run_in_executor(pool, winnowing_compare, text_a, text_b) s = await loop.run_in_executor(pool, structural_compare, text_a, text_b, type_a, type_b) e = await loop.run_in_executor(pool, semantic_compare, text_a, text_b) total = _aggregate(w, s, e, type_a) # existing weight logic return PairScore(winnowing_score=w, jaccard_score=s, semantic_score=e, total_score=total, highlights=_get_highlights(text_a,text_b)) /compare-batch endpoint specification @app.post(&apos;/compare-batch&apos;, response_model=BatchComparisonResponse) async def compare_batch(files: List[UploadFile] = File(...)): # Guard: 2 &lt;= n &lt;= MAX_BATCH_FILES n = len(files) if n &lt; 2 or n &gt; int(os.getenv(&apos;MAX_BATCH_FILES&apos;, 30)): raise HTTPException(400, detail=&apos;Requires 2–30 files&apos;) # 1. Parse all files (reuse existing parse_file() helper) parsed = [parse_file(f) for f in files] # (text, file_type) tuples students = [StudentMeta(student_index=i, name=_name_from_filename(files[i].filename), filename=files[i].filename) for i in range(n)] # 2. Generate all unique pairs and run concurrently pairs = list(itertools.combinations(range(n), 2)) tasks = [_run_pair(*parsed[i], *parsed[j]) for i,j in pairs] results = await asyncio.gather(*tasks) # 3. Fill n×n matrix (None on diagonal) matrix = [[None]*n for _ in range(n)] for (i,j), res in zip(pairs, results): matrix[i][j] = res matrix[j][i] = res # symmetric # 4. Build summary sorted by max_score desc summary = sorted([_build_summary(i, matrix, n) for i in range(n)], key=lambda s: s.max_score, reverse=True) return BatchComparisonResponse(students=students, matrix=matrix, summary=summary) /ws/batch-progress WebSocket specification # Token validation guard (prevents unauthenticated connections) @app.websocket(&apos;/ws/batch-progress&apos;) async def batch_progress_ws(ws: WebSocket, token: str = Query(...)): if not _validate_session_token(token): await ws.close(code=1008) # Policy Violation return await ws.accept() # Client sends: { &apos;session_id&apos;: str } # Server emits: { &apos;completed&apos;: int, &apos;total&apos;: int, &apos;current_pair&apos;: [i,j] } # Use asyncio.Queue stored in a module-level dict keyed by session_id # _run_pair() puts progress updates into the queue while processing ... # Add to requirements.txt: # websockets==12.0 # python-jose==3.3.0 (for token validation) Additional required additions to main.py: Import: import itertools, asyncio, os, concurrent.futures Startup event: load sentence-transformers model once into module-level variable MODEL Thread pool: pool = concurrent.futures.ThreadPoolExecutor(max_workers=4) at module level Helper _name_from_filename(filename): strip extension, replace underscores/hyphens with spaces, title-case Helper _validate_session_token(token): check against a simple env-var secret for now (BATCH_WS_SECRET) Helper _build_summary(i, matrix, n): compute max_score, avg_score, risk_level for student i P1-C [NEW] frontend/next.config.ts ← was missing from original plan This file is required for environment variable exposure, the FastAPI proxy (prevents CORS in production), and the image domain allowlist. next.config.ts — complete file import type { NextConfig } from &apos;next&apos;; const nextConfig: NextConfig = { env: { NEXT_PUBLIC_BACKEND: process.env.NEXT_PUBLIC_BACKEND ?? &apos;fastapi&apos;, NEXT_PUBLIC_FASTAPI_URL: process.env.NEXT_PUBLIC_FASTAPI_URL ?? &apos;http://localhost:8000&apos;, MAX_BATCH_FILES: process.env.MAX_BATCH_FILES ?? &apos;30&apos;, }, async rewrites() { return [ { source: &apos;/fastapi/:path*&apos;, destination: `${process.env.NEXT_PUBLIC_FASTAPI_URL}/:path*` }, ]; }, images: { domains: [&apos;localhost&apos;] }, }; export default nextConfig; // Also create .env.local (gitignored) with: // NEXT_PUBLIC_BACKEND=fastapi // NEXT_PUBLIC_FASTAPI_URL=http://localhost:8000 // MAX_BATCH_FILES=30 // BATCH_WS_SECRET=replace_with_random_32_char_string P1-D API Routes &amp; Abstraction Layer [NEW] frontend/src/app/api/compare-batch/route.ts Acts as a secure server-side proxy. The client never calls FastAPI directly, so the FastAPI URL is never exposed to the browser. compare-batch/route.ts import { NextRequest, NextResponse } from &apos;next/server&apos;; export async function POST(req: NextRequest) { const formData = await req.formData(); const fastapiUrl = process.env.NEXT_PUBLIC_FASTAPI_URL; try { const upstream = await fetch(`${fastapiUrl}/compare-batch`, { method: &apos;POST&apos;, body: formData, }); if (!upstream.ok) { const err = await upstream.text(); return NextResponse.json({ error: err }, { status: upstream.status }); } const data = await upstream.json(); return NextResponse.json(data); } catch (e) { return NextResponse.json({ error: &apos;Backend unreachable&apos; }, { status: 502 }); } } [NEW] frontend/src/lib/backend.ts + backends/fastapi.ts + backends/firebase.ts lib/backend.ts — factory and interface import type { ComparisonResponse } from &apos;@/types/comparison&apos;; import type { BatchComparisonResponse } from &apos;@/types/batch&apos;; export interface BackendService { compare(fileA: File, fileB: File): Promise&lt;ComparisonResponse&gt;; compareBatch(files: File[]): Promise&lt;BatchComparisonResponse&gt;; } // Lazy imports to avoid bundling both backends export async function getBackend(): Promise&lt;BackendService&gt; { if (process.env.NEXT_PUBLIC_BACKEND === &apos;firebase&apos;) { const { FirebaseBackend } = await import(&apos;./backends/firebase&apos;); return new FirebaseBackend(); } const { FastAPIBackend } = await import(&apos;./backends/fastapi&apos;); return new FastAPIBackend(); } backends/fastapi.ts export class FastAPIBackend implements BackendService { async compare(fileA: File, fileB: File): Promise&lt;ComparisonResponse&gt; { const fd = new FormData(); fd.append(&apos;file_a&apos;, fileA); fd.append(&apos;file_b&apos;, fileB); const res = await fetch(&apos;/api/compare&apos;, { method: &apos;POST&apos;, body: fd }); if (!res.ok) throw new Error(await res.text()); return res.json(); } async compareBatch(files: File[]): Promise&lt;BatchComparisonResponse&gt; { const fd = new FormData(); files.forEach(f =&gt; fd.append(&apos;files&apos;, f)); const res = await fetch(&apos;/api/compare-batch&apos;, { method: &apos;POST&apos;, body: fd }); if (!res.ok) throw new Error(await res.text()); return res.json(); } } backends/firebase.ts — fully typed stub (prevents TypeScript drift) // Stub: swap with real Firebase callable when ready. // All method signatures MUST stay in sync with BackendService interface. export class FirebaseBackend implements BackendService { async compare(_fileA: File, _fileB: File): Promise&lt;ComparisonResponse&gt; { // TODO: import { getFunctions, httpsCallable } from &apos;firebase/functions&apos; // const fn = httpsCallable(functions, &apos;compareDocuments&apos;); // return fn({ ... }) as Promise&lt;ComparisonResponse&gt;; throw new Error(&apos;FirebaseBackend.compare not yet implemented&apos;); } async compareBatch(_files: File[]): Promise&lt;BatchComparisonResponse&gt; { throw new Error(&apos;FirebaseBackend.compareBatch not yet implemented&apos;); } } P1-E [NEW] Batch UI Components [NEW] components/batch/BatchUploadZone.tsx ← extracted from page (was missing) Separating the upload zone into its own component keeps batch/page.tsx readable and makes the zone independently testable. Props: onFilesAccepted(files: File[]): void, maxFiles: number Features: drag-and-drop via HTML5 API, click-to-browse fallback, file type guard (.py .js .txt .pdf .docx), file count badge, remove-individual-file button Show a filename chip for each accepted file. Derive student name from filename (strip extension, split on _ or -) Framer Motion: each chip animates in with layout animation (AnimatePresence + motion.div) If files.length &gt; maxFiles: show inline error badge, disable submit CTA Do NOT put axios/fetch calls in this component. It is display-only. [NEW] components/batch/BatchErrorBoundary.tsx ← was missing from original plan Wraps StudentMatrix so a single-file parse failure cannot crash the entire /batch page. BatchErrorBoundary.tsx export class BatchErrorBoundary extends React.Component&lt; { children: React.ReactNode }, { hasError: boolean; error: Error | null } &gt; { state = { hasError: false, error: null }; static getDerivedStateFromError(error: Error) { return { hasError: true, error }; } render() { if (this.state.hasError) return ( &lt;div className=&apos;p-6 rounded-xl border border-red-300 bg-red-50 text-red-700&apos;&gt; &lt;p className=&apos;font-semibold&apos;&gt;Comparison failed&lt;/p&gt; &lt;p className=&apos;text-sm&apos;&gt;{this.state.error?.message}&lt;/p&gt; &lt;/div&gt; ); return this.props.children; } } [NEW] components/batch/StudentMatrix.tsx Renders the N×N pairwise heatmap grid. Full specification from SOP §5.2, with corrected prop interface: Props: data: BatchComparisonResponse, onStudentClick(studentIndex: number): void Grid: CSS Grid with auto columns. Column/row headers = student names. Cell colour: score &lt; 0.30 → bg-green-100, 0.30–0.60 → bg-yellow-100, 0.61–0.79 → bg-orange-100, &gt;= 0.80 → bg-red-200 Diagonal: render &apos;--&apos; in slate-300, no click handler Hover tooltip (Tailwind group-hover): shows winnowing / structural / semantic breakdown Click: calls onStudentClick(row_index) — the panel shows the clicked student&apos;s data Sort toggle: button to sort student rows by max_score desc / alphabetical Export: &apos;Download CSV&apos; button — generates pairwise matrix as CSV client-side (no API call) [NEW] components/batch/StudentDetailPanel.tsx Side-drawer with 3 tabs. Critical correction: Tab 2 must explicitly map PairScore fields to SplitView props — the original plan omitted this mapping. StudentDetailPanel — corrected prop mapping for Tab 2 (SplitView) // The existing SplitView expects these props: // highlightData: HighlightInfo[] // sourceText: string // targetText: string // From BatchComparisonResponse, get the worst-case pair for this student: const worstPair = getWorstPair(studentIndex, batchResponse.matrix); // worstPair is a PairScore — it has a .highlights field // Correct mapping: &lt;SplitView highlightData={worstPair.highlights} // HighlightInfo[] — same type sourceText={parsedTexts[studentIndex]} // string — student&apos;s file text targetText={parsedTexts[worstPeer.index]} // string — worst-match peer text /&gt; // NOTE: parsedTexts must be returned from the backend in BatchComparisonResponse // Add: parsed_texts: List[str] to BatchComparisonResponse schema (P1-A) Remaining StudentDetailPanel specifications: Props: studentIndex: number, data: BatchComparisonResponse, parsedTexts: string[], onClose(): void Tab 1 — Score Overview: renders &lt;Dashboard&gt; with worst-case PairScore injected as scores prop Tab 2 — Side-by-Side: renders &lt;SplitView&gt; using mapping above Tab 3 — All Comparisons: table of all peers sorted by pairwise score desc + mini progress-bar per row Animation: Framer Motion — initial={{ x: &apos;100%&apos; }} animate={{ x: 0 }} exit={{ x: &apos;100%&apos; }} Backdrop: semi-transparent slate-900/60 overlay behind the panel, click-away closes Header: student name, filename chip, risk-level badge (colour matches matrix cell colour) P1-F [NEW] frontend/src/app/batch/page.tsx The batch route page wires everything together. Keep this file thin — all complexity lives in the sub-components. Imports: BatchUploadZone, BatchErrorBoundary, StudentMatrix, StudentDetailPanel State: files[], batchResult: BatchComparisonResponse|null, selectedStudent: number|null, isLoading: boolean, progress: {completed,total} On mount: open WebSocket to /ws/batch-progress?token=SESSION_TOKEN; update progress state on each message On submit: call getBackend().compareBatch(files), set batchResult Render sequence: (1) BatchUploadZone, (2) progress bar (visible only when isLoading), (3) BatchErrorBoundary &gt; StudentMatrix, (4) StudentDetailPanel (conditional) SEO: export const metadata = { title: &apos;Batch Compare | AuraDiff&apos;, description: &apos;...&apos; } PHASE 2 — Frontend, SEO &amp; Branding Begin Phase 2 only after Phase 1 is verified. Items in Phase 2 that depend on Phase 1 are marked with a dependency flag. P2-A Configuration [NEW] frontend/src/styles/tokens.css — CSS variables per SOP §1.1 design token table [MODIFY] frontend/src/app/layout.tsx — Next.js Metadata API (see SOP §3.1), import tokens.css globally [NEW] frontend/public/logo.svg — SVG wordmark (AURA in Indigo-600, DIFF in Cyan-500) [NEW] frontend/src/app/sitemap.ts — dynamic sitemap including /, /compare, /batch, /about [NEW] frontend/src/app/robots.ts — allow all, disallow /api/* P2-B Landing Page Overhaul (frontend/src/app/page.tsx) Hero section — Framer Motion stagger headline, particle background, spring-physics dashboard preview Algorithm Showcase — 3 live-demo cards (Winnowing / Structural / Semantic per SOP §2.2) Statistics bar — CountUp hook, animated on scroll-enter Creator section — 4 team cards with stagger delay (Sandeep 0s, Mahathi 0.1s, Lokesh 0.2s, Pramod 0.3s) All sections: Framer Motion sectionVariant (initial opacity:0 y:32 → visible opacity:1 y:0) Dependency flag: batch CTA button links to /batch — requires Phase 1 complete P2-C SEO JSON-LD: SoftwareApplication schema on layout, FAQPage on /about, BreadcrumbList on /compare and /batch next/font: load Inter with display:swap, pass to --font-sans token All Next.js &lt;Image&gt; components: add priority prop to above-fold images Core Web Vitals: set explicit min-height on all animated section containers to prevent CLS PHASE 3 — Infrastructure &amp; Scaling P3-A [NEW] backend/Dockerfile Production Dockerfile FROM python:3.11-slim WORKDIR /app COPY requirements.txt . RUN pip install --no-cache-dir -r requirements.txt COPY . . # Pre-download the sentence-transformers model at build time RUN python -c &quot;from sentence_transformers import SentenceTransformer; SentenceTransformer(&apos;all-MiniLM-L6-v2&apos;)&quot; EXPOSE 8000 CMD [&quot;gunicorn&quot;, &quot;-k&quot;, &quot;uvicorn.workers.UvicornWorker&quot;, &quot;-w&quot;, &quot;4&quot;, &quot;-b&quot;, &quot;0.0.0.0:8000&quot;, &quot;main:app&quot;] P3-B [MODIFY] backend/requirements.txt ← was missing from original plan Add and pin: websockets==12.0 Add and pin: python-jose==3.3.0 Add and pin: httpx==0.27.0 (used for any outbound requests) Verify existing: sentence-transformers==2.7.0, fastapi, uvicorn, gunicorn — pin all P3-C Monitoring Add Sentry SDK to both Next.js (sentry.client.config.ts) and FastAPI (main.py) Add Vercel Analytics: import { Analytics } from &apos;@vercel/analytics/react&apos; in layout.tsx Verification Plan (Revised) Run verifications in order. Do not advance to the next phase until all checks in the current phase pass. Phase 1 Verifications Run tests/test_weight_sum.py — assert winnowing + structural + semantic weights sum to 1.0 ← NEW, was missing Start FastAPI: uvicorn main:app --reload. Send 3 files to POST /compare-batch. Assert response shape matches BatchComparisonResponse schema. Run python tests/test_batch_ws.py — connects WebSocket, sends session_id, asserts progress messages arrive as { completed, total, current_pair }. Assert /compare (existing) still returns ComparisonResponse unchanged — regression check. Run next build — assert no TypeScript errors in batch/ or lib/backends/ Phase 2 Verifications Open /batch in browser. Upload 3 dummy files (e.g. hello.py, world.py, sample.txt). Confirm matrix renders with correct colour cells. Click a student name. Confirm StudentDetailPanel slides in from the right. Confirm all 3 tabs render without errors. Verify Tab 2 (SplitView) shows highlights coloured correctly (Blue=Exact, Emerald=Structural, Purple=Semantic). Scroll the landing page. Confirm each section fades-in from y:32 on scroll-enter (not on page load). Run Lighthouse on /. Target: Performance &gt;= 85, SEO &gt;= 95. Phase 3 Verifications docker build -t auradiff-backend . — assert build completes, model downloads at build time. docker run -p 8000:8000 auradiff-backend — assert /compare-batch accessible. npx @next/bundle-analyzer — assert first-load JS &lt; 150kB. Master Implementation Checklist Every checkbox below must be ticked before the project is considered complete. Items in red were absent from the original plan. ☐ Phase Task Ref ☐ P1 schemas.py — 4 new models (StudentMeta, PairScore, StudentSummary, BatchComparisonResponse) P1-A ☐ P1 schemas.py — parsed_texts: List[str] added to BatchComparisonResponse ← NEW P1-A ☐ P1 main.py — startup event loads SentenceTransformer model once P1-B ☐ P1 main.py — ThreadPoolExecutor pool at module level P1-B ☐ P1 main.py — _run_pair() calls algorithm functions directly, NOT /compare handler ← NEW P1-B ☐ P1 main.py — POST /compare-batch endpoint P1-B ☐ P1 main.py — WebSocket /ws/batch-progress with token validation ← NEW P1-B ☐ P1 requirements.txt — websockets==12.0, python-jose==3.3.0 pinned ← NEW P1-B ☐ P1 next.config.ts — env vars, FastAPI rewrite proxy, image domains ← NEW P1-C ☐ P1 .env.local created with all required variables ← NEW P1-C ☐ P1 app/api/compare-batch/route.ts — Next.js server-side proxy P1-D ☐ P1 lib/backend.ts — BackendService interface + getBackend() factory P1-D ☐ P1 lib/backends/fastapi.ts — fully typed implementation P1-D ☐ P1 lib/backends/firebase.ts — fully typed stub (throws, not silent) ← NEW P1-D ☐ P1 BatchUploadZone.tsx — extracted as separate component ← NEW P1-E ☐ P1 BatchErrorBoundary.tsx — wraps StudentMatrix ← NEW P1-E ☐ P1 StudentMatrix.tsx — N×N heatmap, click handler, CSV export P1-E ☐ P1 StudentDetailPanel.tsx — 3-tab drawer with corrected SplitView prop mapping ← NEW P1-E ☐ P1 app/batch/page.tsx — wires all components, WebSocket progress P1-F ☐ P1 tests/test_weight_sum.py passes ← NEW Verify ☐ P2 styles/tokens.css — all design tokens P2-A ☐ P2 logo.svg in /public P2-A ☐ P2 layout.tsx — Next.js Metadata API, JSON-LD P2-A ☐ P2 sitemap.ts + robots.ts P2-A ☐ P2 page.tsx — hero, algo showcase, stats, creator section, scroll animations P2-B ☐ P3 backend/Dockerfile P3-A ☐ P3 Bundle &lt; 150kB first-load JS P3-C AuraDiff Revised Implementation Plan v2.1 — Batch-First Edition. Vignan Institute of Technology and Science, CSE Department. P. Sandeep · Mahathi · Lokesh · Pramod.